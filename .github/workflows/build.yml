name: Build AppImage

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y desktop-file-utils libgdk-pixbuf2.0-dev fakeroot binutils appstream

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install appimage-builder
        run: pip install appimage-builder

      - name: Prepare AppDir
        run: |
          # Ensure scripts are executable
          chmod +x AppDir/AppRun
          chmod +x AppDir/run-proton

          # Create a symlink for bash inside the AppDir to satisfy
          # the builder's dependency check for your shell scripts
          mkdir -p AppDir/bin
          ln -s /bin/bash AppDir/bin/bash

      - name: Build AppImage
        shell: bash
        env:
          # This environment variable is the standard way to
          # force the architecture in appimage-builder
          ARCH: x86_64
        run: |
          appimage-builder --skip-tests

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: Proton-Standalone-AppImage
          path: ./*.AppImage
